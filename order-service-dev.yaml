#Server port
server:
  port: 9090

# This is the dev config for order-service
my:
  custom:
    message: "Hello from Config Server (DEV Profile)!"

#Log Pattern
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"    

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/